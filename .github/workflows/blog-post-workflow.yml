name: Latest blog post workflow

on: 
  schedule:
    - cron: '0 * * * *'
  workflow_dispatch:  # Allow manual triggering

jobs: 
  update-readme-with-blog: 
    name: Update this repo's README with latest blog posts
    runs-on: ubuntu-latest

    steps: 
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Run blog post workflow
        uses: gautamkrishnar/blog-post-workflow@master
        with: 
          max_post_count: "4"
          feed_list: "https://sourish.xyz/rss.xml"

      - name: Commit and push changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add README.md
          git commit -m "Updated with the latest blog posts" || echo "No changes to commit"
          git remote set-url origin https://x-access-token:${{ secrets.PERSONAL_TOKEN }}@github.com/sourize/sourize.git
          git push origin main
